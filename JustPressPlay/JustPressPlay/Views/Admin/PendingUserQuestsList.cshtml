@{
    ViewBag.Title = "PendingUserQuestsList";
}
@model JustPressPlay.ViewModels.PendingUserQuestsListViewModel
<h2>PendingUserQuestsList</h2>
This feature is still under development
@*
<div id="adminBody">

    <div class="row wide">

        <div class="large-3 columns">

            @{ Html.RenderPartial("_AdminNavigation"); }

        </div>

        <div class="large-9 columns">

            <div class="panel">
                <h2>EditAchievementList</h2>
            </div>

            <div class="panel">
                <ul>
                    @foreach (JustPressPlay.ViewModels.PendingUserQuestsListViewModel.PendingUserQuest pendingUserQuest in Model.PendingUserQuests)
                    {
	                    <li>@Html.ActionLink(pendingUserQuest.Title, "ManageUserQuestSubmission", new { id = pendingUserQuest.ID })</li>	
                    }
                </ul>
            </div>

        </div>

    </div>
</div>*@